{
  "name": "showdown-replay-studio",
  "version": "1.1.0",
  "description": "Professional tool to record and download Pokemon Showdown replays.",
  "main": "main.js",
  "bin": "main.js",
  "scripts": {
    "start": "node main.js",
    "postinstall": "node scripts/postinstall.js",
    "pkg": "pkg . --public",
    "build:win": "pkg . --targets node18-win-x64 --output dist/showdown-studio-win.exe --compress GZip",
    "bundle:win": "node scripts/bundle_win.js",
    "package:win": "npm run build:win && npm run bundle:win",
    "build:mac": "pkg . --targets node18-mac-x64 --output dist/showdown-studio-mac --compress GZip",
    "bundle:mac": "node scripts/bundle_mac.js",
    "package:mac": "npm run build:mac && npm run bundle:mac",
    "build:linux": "pkg . --targets node18-linux-x64 --output dist/showdown-studio-linux --compress GZip",
    "bundle:linux": "node scripts/bundle_linux.js",
    "package:linux": "npm run build:linux && npm run bundle:linux",
    "build:all": "mkdir -p dist && npm run package:win && npm run package:mac && npm run package:linux"
  },
  "pkg": {
    "assets": [
      "public/**/*",
      "node_modules/puppeteer-stream/extension/**/*"
    ],
    "scripts": [],
    "outputPath": "dist"
  },
  "dependencies": {
    "express": "5.2.1",
    "open": "8.4.2",
    "puppeteer": "24.4.0",
    "puppeteer-core": "^24.4.0",
    "puppeteer-stream": "3.0.23",
    "socket.io": "4.8.3"
  },
  "devDependencies": {
    "pkg": "5.8.1"
  }
}
